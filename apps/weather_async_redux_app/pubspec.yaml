name: weather_async_redux_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  async_redux: ^24.0.2
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_keyboard_visibility: ^6.0.0
  weather_repository:
    path: ../../packages/weather_repository

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
